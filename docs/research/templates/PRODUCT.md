<!--
  TEMPLATE — copy to docs/research/<hostname>/PRODUCT.md.
  Used ONLY in /refine (enhance) mode, adapted from impeccable's PRODUCT.md.
  Pure 1:1 cloning does NOT need this file. It captures intent so enhancements
  stay on-brand instead of drifting into generic "AI slop".
-->

# Product brief — <name>

## Users

`<who uses this, in what context, e.g. "SREs on call, reading fast, often in the dark">`

## Purpose

`<what the interface is for — the one job it must do well>`

## Brand voice

`<tone, e.g. "calm, clinical, no hype">`

## Anti-references (what to avoid)

`<e.g. purple gradients, glassmorphism, "boost your productivity", card-grid-on-card-grid>`

## Enhancement dials (0–10)

- **DESIGN_VARIANCE:** `<0 centered/clean · 10 asymmetric/expressive>` = `?`
- **MOTION_INTENSITY:** `<0 hover only · 10 scroll/magnetic/scenes>` = `?`
- **VISUAL_DENSITY:** `<0 spacious · 10 dashboard-dense>` = `?`

## Scope of enhancement

`<which sections may be improved; which must stay exactly 1:1 with the original>`
