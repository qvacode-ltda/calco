{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": [".astro/types.d.ts", "**/*"],
  // examples/* are self-contained projects with their own tsconfig/alias scope.
  "exclude": ["dist", "examples"]
}
